{% from "components/anchor/anchor.twig" import anchor %}

<div class="lhl-violator">
  <div class="lhl-violator-container">
    <h2 class="lhl-violator-description">{{ widget.description }}</h2>
    <div class="lhl-violator-cta">
      {{ anchor({ text: widget.cta.title, href: widget.cta.url , classes: ['lhl-violator-link'] }) }}
    </div>
  </div>
</div>